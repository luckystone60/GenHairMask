# 2p 样例实验记录

## 环境与输入

- Windows，RTX 2060 SUPER 8GB。
- Python 3.12.13，PyTorch 2.11.0+cu128，Transformers 5.13.1。
- BOK：2509×3760；EDOF：3191×4780。
- 模型缓存总量：约 1.93 GiB。

## 基础模型结果

BiRefNet HR-matting 能清楚保留两人轮廓上的灰白碎发和半透明发丝。Sapiens2-0.4B 能稳定识别男士灰色假发及女士两侧灰发；白色主体头饰被判作 Apparel，这是合理的语义约束，最终严格模式因此不会把整顶头饰或黑色羽饰当成头发。

## 迭代记录

| 版本 | 最终像素 | 观察与决定 |
|---|---:|---|
| v1 | 126,927 | 低 Hair 概率区域扩张过宽，头饰、脸、衣服和袖子轮廓严重误检，否决。 |
| v2 | 46,842 | 只从强 Hair 种子扩张，误检下降，但缺完整 29 类标签，仍含非头发轮廓，否决。 |
| v3 | 3,653 | 用 Sapiens2 全标签排除非 Hair 类，精度高但过于保守，只剩发梢。 |
| v4 | 7,907 | 降低分数/alpha 阈值，召回略升，根部仍被粗 mask 截断。 |
| v5 | 8,806 | 进一步放宽没有解决根因；确认问题是完全排除了粗 Hair 区域。 |
| v6 | 40,320 | 改为保留 Hair 内侧 12px 边缘带，恢复整根飘丝，但边界环偏宽。 |
| v7 | 5,258 | 9×9 开运算差分过严，许多真实发丝被删，否决。 |
| v8/base | 22,378 | 13×13 开运算差分较稳健，但宽结构相减把 17 个候选连通域切成大量碎段。 |
| v9 | 35,995 | 开运算半径由 6 调为 8，减少真实细长区域误删；连通域由 88 降为 32。 |
| v10 | 36,444 | 在已验证候选内部做四方向短缺口连接，补回 449 像素；连通域进一步降为 26，未见明显背景扩张。 |
| v11/balanced | 42,074 | 固定种子颜色锚定、方向一致性和宽度约束的区域生长新增 5,630 像素（+15.4%），主要延伸已有发丝。 |
| v11/recall | 48,644 | 高召回预设较 v10 增加 12,200 像素（+33.5%）；适合漏检明显的数据，但需用 blending 抽检复杂背景。 |
| v12/adaptive | 41,989 | 2p 的常规/延伸半径由固定 120/32px 自适应为 183/142px；该样例没有越界长发丝，主要用于验证无回归和 ROI 等价性。 |

## 最终验收

- 四张基础图及最终 mask/alpha 全部为 2509×3760。
- `fine_hair_mask_01.png` 仅含 0/1，兼容文件 `fine_hair_mask.png` 仅含 0/255；默认平衡档细发丝共 42,074 像素，占全图约 0.446%。
- `2p_bok_mat4k.png` 和 `fine_hair_alpha_16bit.png` 均为 uint16。
- 可视化人工检查：男士左侧、发顶与发尾的飘散丝，以及女士脸右侧/肩部外缘灰发被保留；白色头饰、脸部、黑衣、右侧墙体未进入主 mask。

## 已知边界

- Sapiens2 把白色主体头饰判为 Apparel，因此严格结果只保留其附近被 Hair 语义支持的真实灰发，不把头饰外轮廓算作发丝。
- 二值 mask 会丢失半透明覆盖率；合成或 matting 下游应优先使用 16-bit alpha。

## v2 工程化优化

- 增加严格 `{0,1}` mask、兼容 `{0,255}` mask、16-bit alpha 和完整 BOK 红色 blending。
- 增加 `--prefix` 自动输入发现、`--batch` 目录批处理和失败汇总。
- Hair mask 缺失或为空时使用 matte 作为 ROI 后备；所有 Hair 语义均缺失时才使用 matte 构造种子，并在 metadata 明确记录退化状态。
- 根据 Hair/matte 外接框裁剪重型后处理，自动外扩完整滤波 halo，最终回填原图尺寸。
- Debug 统一放入 `debug/`，当前按 `00_`～`27_` 标识处理顺序。
- ROI 模式对 2p 的实际处理区域由 2509×3760 降至 1858×1412，最终 mask 与整图模式逐像素完全一致。
- 回归测试覆盖 prefix、旧显式 CLI、ROI/整图一致性、无 Hair matte fallback、双样本批处理及输出位深/值域。

## v3 连通性优化

- 最大断裂来源是末端宽结构剔除，而不是双阈值过高：简单降低阈值会增加碎片和背景边缘，不能有效恢复连续性。
- 默认 `thin-radius` 从 6 调为 8，2p 检出像素较 base 增加 60.9%，连通域减少 63.6%。
- 增加四方向短缺口连接；桥接像素必须同时位于 `before_thin`，并满足综合分数，或同时满足线状响应与最低 alpha，且 EDOF/BOK 虚化负证据不能过强。
- 最终结果较 base 增加 62.9%，但距离粗 Hair 超过 30 像素的结果占比由 2.59% 降到 1.59%，新增区域主要集中在已有头发邻域。
- 2p 默认结果为 36,444 像素、26 个连通域；桥接本身只新增 449 像素，避免用无约束膨胀制造粗线或跨入背景。

## v4 受约束区域生长

- 以生长前的最终细发丝为固定种子，用距离变换定位每个候选像素最近的原始种子，并在 Lab 空间比较颜色；颜色不向新像素级联传播，避免区域生长逐步漂移到背景。
- 候选必须具有中值残差细线响应和结构张量方向一致性，并通过 BiRefNet alpha、原综合分数或 Hair 语义之一；EDOF 相对 BOK 的虚化损失仍作为负证据。
- 每轮只从当前结果的八邻域向外生长，并限制邻域密度；形态学宽结构检测持续删除可能形成大块的新增区域。
- Face/Apparel/Clothing 等硬排除区仅允许在原种子 8 像素内由更严格的颜色、线状、方向和 alpha 条件短距离穿过，以兼顾贴脸发丝和误检控制。
- 提供 `off/conservative/balanced/recall` 四档。关闭生长时与 v3 的 36,444 像素逐像素一致；默认 ROI 与整图模式结果逐像素一致。
- 新增 debug 17～23，分别展示生长前结果、颜色相似度、方向一致性、允许区域、新增区域、生长置信度和最终 mask。

## v5 自适应搜索与远距离延伸

- 不取消 search region。全图无上限生长在背景存在同色线条、树枝、电线或服装纹理时风险不可控。
- 常规搜索半径根据图像分辨率和主要 Hair 连通域最长边自适应计算；默认比例为 0.18，并具有分辨率相关硬上限。
- 区域生长另有独立延伸半径，`conservative/balanced/recall` 默认使用 Hair 尺度的 0.08/0.14/0.22，上限分别为 160/240/360 个 4K 参考像素。
- 超出常规搜索区的候选使用更严格门槛：Lab 色差降为常规阈值的 72%，方向一致性提高 0.10，线状响应提高 25%，虚化负证据上限收紧 10%，且必须仍有 alpha 或 Hair 语义支持。
- ROI 安全外扩同时包含常规搜索、远距离延伸和滤波 halo；2p 自适应 ROI 与整图结果逐像素一致。
- 固定模式 `--search-mode fixed` 可逐像素复现 v11；人工 205px 长细线测试中，种子终点 x=100，结果延伸至 x=246，其中 316 个新增像素位于常规搜索区之外。
- 新增 debug 24～27：远距离搜索区、常规区外实际新增、严格延伸候选和延伸区细线响应。

## Hair Bokeh 背景化

- 直接在 mask 内使用中值滤波虽然能压掉细线，但靠近人物轮廓时会同时采样主体和背景，容易产生灰边、色块或轮廓收缩。
- 默认使用 Telea 从 mask 周围的原始 BOK 像素重建背景；原背景已具有散景形态，因此只增加轻微高斯低通来抑制重建纹理，不重新模拟一套全局散景核。
- 发丝核心完全替换，外扩抗锯齿区域用高斯权重在线性光空间羽化；ROI 外像素保持逐值不变。
- 2p 的 inpaint、hybrid、median 三种模式均完成端到端对比，默认保留视觉上更稳健的 inpaint，另外两种作为可选实验参数。

## v6 EDOF 辅助生长与清晰图修复

- 直接用 EDOF 生长在 2p 上从约 43k 暴增到约 96k，右侧清晰墙面纹理形成大面积网状误检，因此否决“EDOF 替代 BOK”的方案。
- 默认改为 `growth-image=dual`：最终坐标和 Lab 颜色锚点始终使用 BOK；EDOF 细线响应做 3px 局部最大值容差，并与 BOK 响应取几何均值，只有两图都存在证据时才明显增强。
- 完整保留 BOK-only 生长结果；仅对 EDOF 额外贡献执行 15×15 局部密度过滤和连通性重检。2p 从 BOK-only 43,296 增至安全 dual 44,504，视觉检查未再出现墙面网状扩张。
- 新增 debug 28～31：EDOF 细线响应、双图融合响应、EDOF rescue 和过密纹理删除结果。
- `hair_bokeh.py --target edof` 使用 Telea 半径 3、1px mask 外扩和 sigma 1.0 羽化，关闭高斯背景模糊；2p 规范分辨率和 3191×4780 原始 EDOF 均完成端到端测试。
- EDOF 与 mask 尺寸不同时，二值 mask 最近邻缩放、alpha 双线性缩放；输出保持 EDOF 原分辨率。BOK 目标的旧调用和输出逐像素完全一致。
